import { PageSlideTransition } from './page-slide-transition';
import { ElseTransition } from './else-transition';

export const NWPageTransitions = {
    transitions: [PageSlideTransition, ElseTransition],
};
